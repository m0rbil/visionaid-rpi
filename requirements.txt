# Vision (Gemini)
google-genai>=1.0.0

# Text-to-speech (Google Cloud TTS, Chirp3-HD)
google-cloud-texttospeech>=2.16.0

# Image handling
Pillow>=10.0.0

# Audio playback
pygame>=2.5.2

# Reads API keys from .env
python-dotenv>=1.0.0

# GPIO button - Raspberry Pi only. Without it the program falls back to the
# Enter key, so it is not required on a development machine.
gpiozero>=2.0 ; platform_machine == "aarch64" or platform_machine == "armv7l"

# picamera2 is not pip-installable. It ships with Raspberry Pi OS; if missing:
#   sudo apt install -y python3-picamera2
